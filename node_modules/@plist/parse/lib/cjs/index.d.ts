import { Value, PlistFormat } from '@plist/common';
export declare const detectFormat: (input: string | ArrayBuffer) => PlistFormat;
export declare const parse: (input: string | ArrayBuffer) => Value;
